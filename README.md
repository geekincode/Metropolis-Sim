# 一维 Ising 模型  蒙特卡洛模拟

## 项目简介

本项目使用 **Metropolis 蒙特卡洛方法**模拟一维 Ising 模型（一维自旋链），计算并绘制磁化强度和磁化率随温度的变化关系。

### 物理背景

- **一维 Ising 模型**：描述一条自旋链，每个自旋可以向上（+1）或向下（-1）
- **哈密顿量**：H = -J ∑⟨i,j⟩ σᵢσⱼ（J 为耦合常数）
- **关键特性**：一维系统在任何正温度下都没有相变，但展示平滑的交叉行为

### 实现特点

- ✅ 自己实现 Metropolis 蒙特卡洛抽样算法
- ✅ 使用周期性边界条件
- ✅ 高效的能量变化计算（只计算局部变化）
- ✅ 模块化设计，代码清晰易懂
- ✅ 使用 CMake 管理项目

## 项目结构

```
Physics/MC/
├── src/
│   ├── IsingModel1D.h        # Ising 模型类接口
│   ├── IsingModel1D.cpp      # Ising 模型核心实现
│   ├── Observables.h         # 可观测量计算接口
│   ├── Observables.cpp       # 统计分析实现
│   └── main.cpp              # 主程序
├── data/                     # 输出数据
├── plots/                    # 生成的图表
├── scripts/
│   └── plot.gnu              # Gnuplot 绘图脚本
├── CMakeLists.txt            # CMake 配置
└── README.md                 # 本文件
```

## 编译和运行

### 前置要求

- C++ 编译器（支持 C++17）
- CMake (>= 3.10)
- Gnuplot（用于绘图）

安装 gnuplot：
```bash
sudo apt-get install gnuplot  # Ubuntu/Debian
```

### 编译步骤

```bash
cd /home/geekincode/Physics/MC
mkdir build && cd build
cmake ..
make
```

### 运行模拟

```bash
cd build
./ising1d
```

模拟将运行约 2-5 分钟，数据会自动保存到项目根目录的 `data/` 文件夹。

### 生成图表

```bash
cd /home/geekincode/Physics/MC
gnuplot scripts/plot.gnu
```

图表将保存为 `plots/results.png`。

## 模拟参数

**系统尺寸**：N = 100, 400, 900（多尺寸对比研究）  
**耦合常数**：J = 1.0  
**温度范围**：T = 0.1 ~ 5.0（步长 0.1）  
**平衡化扫描**：5000 次  
**测量扫描**：50000 次  
**测量间隔**：每 10 次扫描

可以在 `src/main.cpp` 中修改这些参数。

## 预期结果

**图 (a) - 磁化强度 vs 温度：**
- **低温（T < 1）**：磁化强度接近 1，自旋大部分对齐
- **高温（T > 3）**：磁化强度接近 0，自旋随机分布
- **关键特征**：不同系统尺寸的曲线几乎重合，体现一维系统无相变的特性

**图 (b) - 磁化率 vs 温度：**
- **低温区**：磁化率较小（系统有序，涨落小）
- **中温区**：磁化率出现峰值（约在 T ~ 0.5）
- **有限尺寸效应**：峰值高度随 N 增大而显著增大
  - N=100: χ_max ~ 25
  - N=400: χ_max ~ 100-150
  - N=900: χ_max ~ 600+
- **高温区**：磁化率按 χ ~ 1/T 衰减

详细的物理解释请参见 `RESULTS.md`。

## 核心算法

### Metropolis 接受准则

```cpp
if (ΔE ≤ 0) {
    接受翻转（总是）
} else {
    以概率 exp(-β*ΔE) 接受翻转
}
```

### 能量变化计算

```cpp
ΔE = 2*J*σᵢ*(σᵢ₋₁ + σᵢ₊₁)
```

只计算局部变化，避免重复计算整个系统能量。

## 扩展方向

- 不同边界条件（开放边界）
- 添加外磁场
- 自关联函数分析
- 二维 Ising 模型（有相变）
